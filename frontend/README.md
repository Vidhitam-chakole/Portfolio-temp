
# Portfolio OS

An interactive Windows-inspired portfolio for Vidhitam Chakole.

## Development

```bash
npm install
npm run dev
```

The production bundle can be checked with `npm run build`. The optional FastAPI service lives in `../backend`.

## Session access

The demo login accepts the password `vidhitam`. Visitor names are kept in the browser session store and are not sent to a server.
