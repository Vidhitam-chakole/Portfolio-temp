# Vidhitam Chakole Portfolio

Portfolio OS is an interactive React desktop experience backed by an optional FastAPI service.

- Frontend: `cd frontend && npm install && npm run dev`
- API: `cd backend && python -m pip install -r requirements.txt && uvicorn main:app --reload`
- Production check: `cd frontend && npm run build`
